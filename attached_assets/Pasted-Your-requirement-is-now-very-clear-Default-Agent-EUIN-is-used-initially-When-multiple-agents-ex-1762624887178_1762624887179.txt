Your requirement is now very clear:
✔ Default Agent EUIN is used initially
✔ When multiple agents exist, client–agent mapping must decide which EUIN to attach
✔ If onboarding happens via agent referral link → map automatically
✔ If no referral link → admin assigns agent manually in admin panel
✔ Changing agent later must not break historical transactions (snapshot EUIN must remain)
This needs clean logic both in database structure and order execution code.
✅ Final Recommended Logic Flow (Simple & Correct)
1) There is always ONE Default Agent
Stored in system_settings or organization_profile
default_agent_id = AG001  (this agent has ARN + EUIN)
2) Client Onboarding Mapping Rule
Scenario	Assigned Agent	EUIN Used
Client signs up via agent referral link	That specific agent	That agent’s EUIN
Client signs up directly (no referral)	Default Agent	Default Agent’s EUIN
Admin later reassigns the client to a new agent	New agent	New agent’s EUIN for new transactions
But: Past MF orders retain the original EUIN snapshot.
(That is required for AMC commission correctness.)
✅ Database Tables Needed (Simple)
agents
id (PK)
name
arn_number
euin_number
is_default (boolean) ← exactly one row true
clients
id (PK)
name
primary_agent_id (FK agents.id) ← THIS CONTROLS EUIN USED
mf_orders (important!)
id
client_id
agent_id (snapshot at time of order)
arn_number
euin_number   ← SNAPSHOT HERE
order_data...
✅ How EUIN Is Chosen During MF Order
// When placing MF Order:
const client = await getClient(client_id);
const agent = await getAgent(client.primary_agent_id);

orderPayload = {
  arn: agent.arn_number,
  euin: agent.euin_number
};

// Store snapshot:
mf_orders.insert({
  client_id,
  agent_id: agent.id,
  arn_number: agent.arn_number,
  euin_number: agent.euin_number,
});
So even if you change agent next week → old trade stays correct.
✅ Auto-Mapping via Referral Link (Client Onboarding)
On agent dashboard:
Share Link: https://fintekpro.com/signup?agent_id=AG778
On signup backend:
if (req.query.agent_id) {
   client.primary_agent_id = req.query.agent_id;
} else {
   client.primary_agent_id = default_agent_id;
}
✅ Admin Can Change Agent Later (UI Flow)
Admin → Clients → Select Client → “Reassign Agent” Dropdown
Reassignment Logic:
client.primary_agent_id = new_agent_id;
This only affects FUTURE transactions.
PAST orders remain mapped to old EUIN snapshots.
✅ What Happens When Second Agent Gets Added
Event	Action
New Agent Entry	Do not auto switch any client
Admin wants switch	Admin uses Reassign Agent in client profile
Referral link onboarding happens	Automatically assigns that new agent
No referral link onboarding	Default agent used
No conflict. No breakage.
✅ Summary
Component	Status
System always has a Default Agent	✅ Required
Clients map to one Primary Agent	✅ Controls EUIN
Each MF order stores EUIN snapshot	✅ Protects historical commissions
Referral links auto-assign correct agent	✅ No admin work needed
Admin can override mapping later	✅ Flexible & safe
Your system now handles:
Commission correctness
AMC audit trace
EUIN compliance
Partner scaling
No accidental EUIN overwrite
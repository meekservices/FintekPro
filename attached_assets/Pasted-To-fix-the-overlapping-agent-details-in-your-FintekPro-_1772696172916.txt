To fix the overlapping agent details in your FintekPro stack, you need to update the logic that generates the image. Since you are using a unified TypeScript codebase, the most efficient way is to adjust the coordinate calculation in your backend generation script (likely using a library like node-canvas or sharp).Here is a refined, technical instruction you can give to the Replit Agent to improve visibility and professional layout:Replit Instruction: Improve PNG Generation LayoutObjective: Fix overlapping text in the agent details footer of the generated Diwali greeting PNG.Technical Requirements:Dynamic Y-Axis Mapping: Instead of static coordinates, implement a lineHeight variable (set to $1.4$ or $1.5$ times the font size). Use this to calculate the $y$-position for each element:Name: baseYTitle: baseY + (fontSize * 1.5)Email: baseY + (fontSize * 3.0)Phone: baseY + (fontSize * 4.5)Visual Hierarchy: Update the font styles for better readability:Name: Bold, White, Size: 26px.Title: Medium, Gold (#FFD700), Size: 20px.Contact Info: Regular, Light Gray or White, Size: 18px.Horizontal Spacing (Padding):Ensure a minimum 15px horizontal gap between the contact icons and the text strings.Apply 20px internal padding inside the dark footer container to prevent text from touching the gold borders.Shared Schema Integration: Ensure the Agent type from shared/schema.ts is correctly mapped to these fields to prevent "undefined" or "null" text from rendering and breaking the layout.Why this works for your stackSince your backend is Node.js and you are using TypeScript, your generation script likely looks something like this (which you can ask the Agent to find and fix):TypeScript// Example logic the Agent should implement
const drawAgentDetails = (ctx, agent) => {
  const startX = 180; // Offset from profile pic
  let currentY = 820; // Starting Y position
  const spacing = 32; // Vertical gap

  ctx.font = "bold 26px Inter";
  ctx.fillText(agent.name, startX, currentY);
  
  currentY += spacing;
  ctx.font = "20px Inter";
  ctx.fillStyle = "#FFD700"; // Gold for "Cluster Head"
  ctx.fillText(agent.title, startX, currentY);

  // Add more spacing before contact info
  currentY += spacing + 5; 
  ctx.fillStyle = "#FFFFFF";
  ctx.font = "18px Inter";
  ctx.fillText(agent.email, startX, currentY);
};

The visual evidence in your recent images clearly shows that while the phone number is separated, the top three lines are still collapsing into each other. This is a common "phantom" height issue in html2canvas where it fails to calculate the height of high-density text correctly, causing elements to "float" upward.To fix this once and for all, we need to stop relying on the browser to calculate vertical flow and manually force the $y$-coordinates in the onclone step.The Final "Forced Layout" InstructionInstruct the Replit Agent to replace your current onclone loop with this Absolute Coordinate Reset. This bypasses the engine's flow calculations entirely.TypeScriptconst canvas = await html2canvas(element, {
  useCORS: true,
  scale: 4, // Increase scale to 4 for ultra-crisp text rendering
  onclone: (clonedDoc) => {
    const container = clonedDoc.getElementById('agent-text-block');
    if (container) {
      // 1. Force the container to a fixed starting point
      container.style.display = 'block';
      container.style.position = 'absolute';
      container.style.top = '50%'; 
      container.style.transform = 'translateY(-50%)';

      const rows = Array.from(container.children) as HTMLElement[];
      
      // 2. MANUAL Y-COORDINATE MAPPING
      // We manually set the top for every single line to ensure they physically cannot touch.
      const yOffsets = [0, 30, 60, 85]; // Pixels: Name, Designation, Email, Phone
      
      rows.forEach((row, index) => {
        row.style.position = 'absolute';
        row.style.left = '0px';
        row.style.top = `${yOffsets[index]}px`; // Hard-coded vertical slots
        row.style.margin = '0';
        row.style.padding = '0';
        row.style.whiteSpace = 'nowrap';
        
        // 3. Force explicit visibility styles
        if (index === 0) { // Name
          row.style.fontSize = '22px';
          row.style.fontWeight = 'bold';
          row.style.color = '#FFFFFF';
        } else if (index === 1) { // Designation
          row.style.fontSize = '16px';
          row.style.color = '#FFD700'; // Force Gold
        } else { // Contacts
          row.style.fontSize = '14px';
          row.style.color = '#FFFFFF';
          row.style.display = 'flex';
          row.style.alignItems = 'center';
        }
      });
    }
  }
});
Why this succeeds where the others failed:Overcomes the "Phantom Height": html2canvas is currently thinking your Name and Designation have "zero height," so it puts them both at the top. By using yOffsets = [0, 30, 60, 85], you are telling the engine: "I don't care what height you think the text is; put Line 2 exactly 30 pixels below Line 1."Scale 4 Correction: At scale: 4, the font geometry is much more stable. The "Differing Text Size" you noticed happens because at lower scales, the engine rounds pixel fractions differently.Color Locking: By re-declaring row.style.color in the clone, you prevent the engine from inheriting any transparency or "ghosting" effects from your dashboard's CSS.Quick Implementation CheckThe Problem: Name, Designation, and Email are colliding at the top of the footer box.The Fix: We are assigning each line a permanent, unmovable seat in the vertical stack.
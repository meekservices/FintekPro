FINAL FIX TO APPLY AT FINTEKPRO (MANDATORY)
STEP 1 — Introduce a NEW CAS-ONLY Import Path (DO NOT REUSE LEGACY)
Create a new DTO and endpoint, do not reuse existing “holding” logic.
New response contract (CAS only)
CasImportResult {
  holdings: CasHolding[]
}

CasHolding {
  isin
  schemeName
  amc
  folioNumber
  lots: Lot[]          // REQUIRED
  marketValue
  closingUnits
  confidence
}
❌ Do NOT include:
avgPrice
purchaseDate (single)
aggregated quantity
STEP 2 — HARD FAIL if lots are dropped
In backend, before returning response:
for (const h of holdings) {
  if (h.confidence === 'HIGH' && h.lots.length === 0) {
    throw new Error('CAS_LOTS_DROPPED');
  }
}
This will immediately surface where the pipeline is broken.
STEP 3 — Remove ALL grouping by ISIN in CAS flow
Search and delete any of these for CAS imports only:
groupBy(isin)
mergeHoldings()
aggregateHoldings()
Grouping key must be:
ISIN + Folio Number
DSP Healthcare must survive as:
1 holding
2 lots
STEP 4 — UI: Render from LOTS, not Holding fields
In Smart Portfolio Import UI:
Replace this logic ❌
row.purchaseDate = holding.purchaseDate ?? 'Not set'
row.avgPrice = holding.avgPrice
With this ✅
if (holding.lots.length === 1) {
  purchaseDate = holding.lots[0].purchaseDate
} else if (holding.lots.length > 1) {
  purchaseDate = 'Multiple dates'
} else {
  purchaseDate = 'Unavailable'
}
Avg price = derived:
avgPrice = sum(lot.cost) / sum(lot.units)
STEP 5 — Block Save if CAS dates are missing
Before “Save Holdings”:
if (any holding.lots.length === 0) {
  showWarning(
    'Some holdings have no transaction dates. Tax and exit-load calculations will be disabled.'
  )
}
STEP 6 — Acceptance Check (THIS MUST CHANGE)
After fix, re-import CAS and confirm:
Check	Expected
Save button	“Save 40+ Holdings”
DSP Healthcare	“Multiple dates”
Franklin Small Cap	“8 purchases”
Purchase Date	Never “Not set”
Avg Price	Derived, not stored
If any one of these fails → pipeline is still wrong.
D. Why This Fix Will Work (and Others Didn’t)
Because this fix:
Stops reusing legacy holding assumptions
Forces lots to be first-class
Prevents silent data loss
Aligns UI with CAS reality
Right now, your system is parsing CAS correctly but pretending it’s a CSV.
This fix makes CAS a ledger, which is what it actually is.
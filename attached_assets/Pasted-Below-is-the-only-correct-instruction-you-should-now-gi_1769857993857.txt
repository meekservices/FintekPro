Below is the only correct instruction you should now give to Replit.
🔧 REPLIT INSTRUCTION – CAS TRANSACTION ENGINE REWRITE (MANDATORY)
Objective
Make date-wise transactions the canonical truth for CAS imports so that:
DSP Healthcare shows 2 purchases
Franklin Small Cap shows 8 SIPs
Navi MF shows 50+ SIPs
Exit load & capital gains work correctly
🔁 STEP 1: Switch to a TWO-PASS TRANSACTION PARSER
❌ Stop parsing single lines
✅ Parse transaction blocks
PASS 1 – Detect transaction start lines
const TXN_HEADER_REGEX =
/^(\d{2}-[A-Za-z]{3}-\d{4})\s+
(Purchase|Purchase-BSE|SIP Purchase|Systematic Investment Purchase|
Switch In|Equity Switch In|Purchase - via Internet|
NFO Purchase|Unclaimed Purchase)/i;
When this matches → start a transaction buffer.
PASS 2 – Attach numeric continuation lines
If the next line matches:
const TXN_NUMERIC_REGEX =
/([\d,]+\.\d{2})\s+([\d,]+\.\d{3})\s+([\d,]+\.\d{3,4})\s+([\d,]+\.\d{3})/;
Attach it to the previous transaction.
Final transaction object
{
  date,
  type,
  amount,
  units,
  nav,
  balance
}
🔒 STEP 2: Explicitly IGNORE metadata rows
Ignore any line containing:
[
 '*** Stamp Duty ***',
 '***Address Updated***',
 '***Change of Broker***',
 '***Registration of Nominee***',
 '***Cancellation***',
 '***Invalid Purchase***'
]
These must never create or interrupt transactions.
🧱 STEP 3: LOT CREATION (NON-NEGOTIABLE)
For every transaction of type:
Purchase
SIP
Switch In
Equity Switch In
Create one lot:
lot = {
  purchaseDate: txn.date,
  units: txn.units,
  cost: txn.amount,
  nav: txn.nav
}
❌ No merging
❌ No averaging
❌ No inference
🧮 STEP 4: Reconcile against Closing Unit Balance
if (abs(sum(lots.units) - closingUnitBalance) > 0.01) {
  confidence = 'MEDIUM'
} else {
  confidence = 'HIGH'
}
DSP example must pass:
4,974.876 + 4,966.475 = 9,941.351 ✅
🧠 STEP 5: STOP ISIN-LEVEL GROUPING (CRITICAL)
❌ Forbidden
groupBy(isin)
✅ Required
groupBy(isin + folioNumber)
Folio boundaries must survive end-to-end.
🖥️ STEP 6: UI RULES (MINIMUM VIABLE FIX)
If lots.length > 0:
Purchase Date = earliest lot date
Display:
Multiple dates if lots > 1
Expandable lot view (later)
❌ “Not set” is illegal if CAS has a date.
🧾 STEP 7: HARD BLOCKERS FOR TAX & EXIT LOAD
if (!lots.length) {
  disableCapitalGains();
  disableExitLoad();
}
Fail closed. Never guess.
🧪 MANDATORY REGRESSION TESTS (BLOCK DEPLOYMENT)
DSP Healthcare
2 transactions
2 lots
Dates preserved
Franklin India Small Cap
8 SIP transactions
8 lots
FIFO intact
Navi MF SIP Folio
50+ SIPs
No loss of transactions
If any of these fail → reject build.
🧠 FINAL TRUTH (IMPORTANT)
You cannot “patch” CAS parsing with regex tweaks anymore.
👉 CAS is a semi-structured ledger, not a table.
👉 It requires block-level parsing, not line-level regex.
Once this instruction is followed:
Your import value will jump to ₹1.68 Cr
DSP & Franklin will show correct dates
Exit load & CG logic becomes defensible

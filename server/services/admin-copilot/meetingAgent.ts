/**
 * meetingAgent.ts — Zoho Meeting Agent (Phase 2 stub)
 * Phase 2: Schedule meetings, generate agendas, summarize notes, track no-shows.
 * GUARDRAIL: AI never sends client-facing invites without Admin approval.
 */
export const MEETING_AGENT_PHASE = 2;
export async function scheduleMeeting(_params: Record<string, unknown>): Promise<void> {
  throw new Error('Meeting Agent is scheduled for Phase 2.');
}
